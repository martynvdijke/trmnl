{% comment %}TRMNL fields: {{ url }} {{ username }} {{ password }}{% endcomment %}
{% if error %}
<div class="layout layout--col gap--small">
  <div class="item">
    <div class="content">
      <div class="value value--small">{{ error }}</div>
    </div>
  </div>
</div>
{% else %}
<div class="layout layout--col gap--small">
  <div class="title_bar">
    <div class="title">NPM Certs</div>
    <div class="meta">{{ count }}</div>
  </div>
  <div class="item">
    <div class="content">
      <div class="value value--medium">{{ count }}</div>
      <div class="label label--small">certificates</div>
      {% if expiring_soon_count > 0 %}
      <div class="meta">{{ expiring_soon_count }} expiring soon</div>
      {% endif %}
    </div>
  </div>
</div>
{% endif %}
